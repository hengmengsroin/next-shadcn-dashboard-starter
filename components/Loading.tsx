import { Colors } from '@/utils/constant';

const Loading = () => {
  return (
    <div className="flex h-[60%] w-full flex-col gap-2">
      <div color={Colors.primary}>Loading...</div>
    </div>
  );
};

export default Loading;
